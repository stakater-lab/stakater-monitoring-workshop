# Source: prometheus-operator/templates/grafana/configmaps-datasources.yaml

apiVersion: v1
kind: ConfigMap
metadata:
  name: NAMESPACE-prometheus-opera-grafana-datasource
  namespace: NAMESPACE
  labels:
    grafana_datasource: "1"
    app: prometheus-operator-grafana
    
    chart: prometheus-operator-8.2.0
    release: "NAMESPACE"
    heritage: "Tiller"
data:
  datasource.yaml: |-
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: prometheus
      url: http://prometheus-NAMESPACE.cp-stakater.com
      access: proxy
      isDefault: true